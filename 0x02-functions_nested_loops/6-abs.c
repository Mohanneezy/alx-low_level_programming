#include "main.h"

/**
 * _abs - checks for absolute
 * @n: n is an ascii character
 *
 * Return: 1 if lowercase
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (0);
}
