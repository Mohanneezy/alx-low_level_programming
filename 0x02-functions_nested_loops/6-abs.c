#include "main.h"

/**
 * _abs - checks for absolute
 * @c: c is an ascii character
 *
 * Return: 1 if lowercase
 */
int _abs(int)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{	
return (-n);
}
else
{	
return (n);
}
return (0);	
}
