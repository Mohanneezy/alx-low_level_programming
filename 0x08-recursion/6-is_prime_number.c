#include "main.h"
/**
 * is_prime_number - Finds if n is prime number.
 * @n: The number to find the square root of.
 *
 * Return: If the number is prime number.
 */

int is_prime_number(int n)
{
if (n % is_prime_number( n-1 ))
return (1);
else
return (0);
}
