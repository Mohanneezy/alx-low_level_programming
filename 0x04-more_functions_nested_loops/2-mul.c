#include "main.h"

/**
 * mult - Checks for a digit (0-9).
 * @a: The number to be checked.
 * @b: The number to be checked.
 *
 * Return: 1 if the number is a digit, 0 otherwise.
 */
int mul(int a, int b)
{
   
	return (a * b ;);
}
